import React from 'react';

const appTextComponent = () => {
    return (
        <header className="App-header">
            <p>This is React + Apollo + GraphQL + NodeJS + MongoDB starter kit</p>
            <hr/>
        </header>
    );
};
export default appTextComponent;